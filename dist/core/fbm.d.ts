import type { FBMConfig } from '../types/config.js';
import type { LLMAdapter, EmbeddingAdapter } from '../types/adapter.js';
import type { MemorySummary } from '../types/retrieval.js';
import type { ConversationMessage } from '../types/conversation.js';
import { MemoryStore } from './store.js';
import { IndexEngine } from './index-engine.js';
import { VectorIndex } from './vector-index.js';
import { MemoryConsolidator } from './memory-consolidator.js';
import { DynamicMemory, type MemoryInjection } from './dynamic-memory.js';
export declare class FBM {
    private config;
    private store;
    private indexEngine;
    private keywordExtractor;
    private nodeLocator;
    private vectorIndex;
    private retriever;
    private consolidator;
    private dynamicMemory;
    private llm;
    private embedding;
    private unwatch;
    private _initialized;
    constructor(config: FBMConfig, llm: LLMAdapter, embedding?: EmbeddingAdapter);
    get initialized(): boolean;
    init(): Promise<void>;
    retrieve(query: string): Promise<MemorySummary>;
    onUserMessage(message: ConversationMessage): Promise<MemoryInjection[]>;
    onAssistantMessage(message: ConversationMessage): Promise<void>;
    getStore(): MemoryStore;
    getIndexEngine(): IndexEngine;
    getVectorIndex(): VectorIndex;
    getConsolidator(): MemoryConsolidator;
    getDynamicMemory(): DynamicMemory;
    shutdown(): Promise<void>;
    private ensureInitialized;
    private rebuildVectors;
    private addVectorsForDir;
    private addVectorsForFile;
    private buildHeadingPath;
}
//# sourceMappingURL=fbm.d.ts.map